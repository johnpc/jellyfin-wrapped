# Jellyfin server configuration
# These environment variables are required for the app to function properly

# The URL of your Jellyfin server (required)
VITE_JELLYFIN_SERVER_URL=http://your-jellyfin-server:8096

# Admin API key for accessing playback reporting data (required)
# This should be an API key from an admin user account
VITE_JELLYFIN_API_KEY=your-admin-api-key-here
 
# Note: The VITE_ prefix is required for Vite to expose these variables to the client
# In production, you can also set these via window.ENV object
